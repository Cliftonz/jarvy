# PR-gated build check for the VS Code extension. No secrets, no publish —
# proves the extension always type-checks, compiles, and packages cleanly.
# The publish path lives in vscode-publish.yml (tag-triggered).
name: VS Code Extension CI

on:
  push:
    paths:
      - editors/vscode/**
      - .github/workflows/vscode-ci.yml
  pull_request:
    paths:
      - editors/vscode/**
      - .github/workflows/vscode-ci.yml
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: vscode-ci-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: editors/vscode

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '26'
          cache: npm
          cache-dependency-path: editors/vscode/package-lock.json

      - name: Install dependencies
        run: npm ci || npm install

      - name: Type-check (strict, no emit)
        run: npm run check

      - name: Compile
        run: npm run compile

      - name: Unit tests (parseValidationResult schema contract)
        run: npm test

      # Run the vsce that package.json declares as a devDependency and npm ci
      # installed, not whatever `npx --yes` resolves as latest at run time.
      # --no-install makes the step fail loudly instead of silently fetching a
      # newer major nobody chose.
      - name: Package (.vsix) — validates the manifest and bundled files
        run: npx --no-install @vscode/vsce package --out jarvy-vscode.vsix

      - name: Upload .vsix for inspection
        uses: actions/upload-artifact@v7.0.1
        with:
          name: jarvy-vscode-vsix-ci
          path: editors/vscode/jarvy-vscode.vsix
          retention-days: 14
